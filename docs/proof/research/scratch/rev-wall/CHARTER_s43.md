# CHARTER (round 7 for this lane): S4.3-GENERAL — THE COMPENSATION CHANNEL

Coordinator, 2026-09-22. Your round 6 is VERIFIED end-to-end and
ACCEPTED (OVERVIEW entry appended): battery byte-identical; the
recursion theorem hand-verified and fresh-encoded (600 cases on
bitten/merged/psi-mapped texts, 0 mismatches); COR-A's cascade
hand-scrutinized including the sharp m = k-1 inequality; all the
witnesses fresh-encoded (the k=2 exception, the resumption {0,2},
the size-1 interior {1} with j = 7,8 boundaries, the COR-A/B
sweeps); the BLOB LANDING hand-derived by me before reading your
machine and fresh-encoded; MASS, E2, E5 fresh-encoded. One
cosmetic note: the recursion theorem's phrase "the window consumes
the last c_sigma atoms" is loose — it consumes the last i (the
remaining c_sigma - i stay as remnant); the formula and uses are
correct.

## Context: the endgame after your round 6

TL is unconditional (Lane B round 6 — PO-1/EPT/PO-3/PO-4 at full
altitude; INV4's bookkeeping at case-analysis altitude, Lane B
round 7 in flight closes it). Lane C's OL-1 (round 20) has landed:
the match condition is two-sided (their Lemma M scan side + Lemma
FIT pattern-side fit window); the fit window kills the cross-pair
families (all fit-ghosts); the mirror forcing lemma pins the
trailing flank to [3^k - BotSum(t), 3^k]; their located gap is the
Delta > 0 multi-hit closure. THE ENDGAME RESIDUE IS NOW: YOUR
S4.3-general (this round), OL-1's Delta > 0 closure (Lane C round
21), and INV4's altitude (Lane B round 7, in flight). If all three
land, Lane A integrates and part (ii) flips to proved.

## Your round: S4.3-general (the digit-matching lemma, unbounded terms)

The single remaining piece of OL-2: the exactness arithmetic in
the CORRECTION regime (bites, empty runs, inserted copies — texts
outside the increasing-run class). Your round-6 statement:

  In the GLUE decomposition v(r) = span - bites + c*rho, exactness
  of the repunit Sum_[0..m] (base-3 digits all 1 on scales 0..m)
  forces PER-SCALE matching: a bite at scale s creates a digit
  defect that cannot cancel against terms at separated scales
  (super-increase), so it must be compensated by R-material at
  scale s + O(1) — a synthetic value at an interior scale, whose
  supply is S4.5's recursion (Omega(min(m, k-m)) or J(V)).

  Proved so far: the psi-map never lands ([a/aa]a^{3^{m+1}} =
  Sum + 1); the only exact synthetic landing is the mod-deletion
  (S4.4, priced). OPEN: the unbounded-term case — compositions of
  MANY small corrections summing to a repunit digit pattern.

Suggested decomposition (think in steps):
1. Formalize the digit-defect lemma: write v in base 3 (or its
   strata normal form); a bite b at scale s contributes -b; the
   span and c*rho terms have their own digit patterns; exactness
   means the TOTAL digit vector is the repunit. The super-increase
   gives: a nonzero defect at the TOP defect scale cannot be
   cancelled by lower scales — so the top defect scale's
   compensation must come from a term AT that scale (R-material or
   a span boundary). Induct downward on the top defect scale.
2. The compensation inventory: R-material at scale s + O(1) (each
   firing's inserted rho carries its own strata — TL now
   unconditional gives you the inventory: anchored exponents, so
   the compensating material at interior scales is ITSELF a
   synthetic value — S4.5 recursion), span boundaries (junction
   moves — priced by the fired-set structure FSL'/class-escape),
   or another bite (the cancellation needs exact scale-matching —
   the lucky-sum channel proper).
3. The lucky-sum closure: your own round-5 map (supply slopes
   rational with V-fixed denominators vs the dyadic demand, B's C2
   + Lane C's new FIT window) + the second-difference route Lane C
   noted (evenly-spaced hits canceling drift) — show the composed
   corrections either (a) reduce to finitely many V-fixed
   scale-coincident pairs (each an S4.5 supply), or (b) leave a
   top defect scale uncompensated. EPT (Lane B round 6) is your
   engine for the eventual periodicity of the case splits.
4. Machine: extend part E with the composition cases — small
   batteries showing compensation pairs landing/not landing at
   interior scales.

If the piece resists, locate it precisely — a located gap is a
round's work; and note this is the LAST piece of OL-2: everything
else in your rounds 4-6 is at altitude.

## Rules (user directives, hard)

- Theory first; no brute force; every run <= 1 minute; C for
  CPU-heavy; log full invocations (first line); scripts + logs +
  report appended to REPORT.md; report back to me.
