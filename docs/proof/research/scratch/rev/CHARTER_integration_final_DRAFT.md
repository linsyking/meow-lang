# CHARTER (Lane A, FINAL INTEGRATION) — DISPATCH NOW: all pieces landed and verified

Coordinator, 2026-09-22, FINAL UPDATE after B9/C21b/D8 verification.
Every named piece has landed AND passed my verification (OVERVIEW
entries of 2026-09-22). The fragment to mount is
research/scratch/rev-try/dichotomy.tex — verified round 18,
integration-ready (my wrapper: 0 errors / 0 undefined refs /
0 overfull / 9 pages in the paper's preamble).

## The residue of part (ii): NONE REMAINS. Final inventory.

ALL LANES CLOSED AND VERIFIED:
  * TL UNCONDITIONAL AT FULL ALTITUDE — the LaTeX to mount is now
    Lane B's ROUND9_REPORT.md SECTION 1 (NOT round 7's section 5,
    which is superseded): the two-color profile clause, the
    replication-corrected expansion exponent (the ADDITIVE nesting
    depth Delta_V, C-R9-1), clause (i)'s ambient-index anchoring
    (C-R9-2), the two-tier directive count (C-R9-3), masks as
    Boolean combinations of residue tests (mod2 period ord_4(3)=2
    and mod7 period ord_7(3)=6 both exhibited), T_V's spelled-out
    domain, Merge uniformly over both colors. I verified the block
    compiles standalone (pdflatex, EXIT 0, zero errors, zero
    undefined refs with amsthm theorem+remark declared — main.tex's
    preamble provides both). MOUNT VERBATIM, with Lane B's notes:
    keep N_V explicit (the tighter literal product NOT certified
    for deep V); their Delta_V = the expansion's depth, their d =
    the S-depth (coordinate with the paper's D_V letters); the
    remark is part of the block (drop its first sentence if
    Section 2 defines [R/P]F).
  * OL-1 LANDED AND CORRECTED (Lane C rounds 20-21b): Lemma M/FIT,
    K2's real-channel rigidity, Lemma S, the trichotomy (a)/(b)/(c);
    round 21's Theorem D (D1 drift-robust mirror forcing, D2 the
    window/debt dichotomy, D3 multi-service pricing, MR mass
    routing with the Payment interface) + Lemma INH (the survivor
    supply bound (3^k+1)/2, the origin trace) + the DISJUNCTIVE
    counting corollary: any (beta)-service costs Omega(k) via
    supply; all-(alpha) forces k single-service channels each
    paying the exact top-scale window (a part-B slot or an exact
    deficit = supply) or the mass debt. Mount the CORRECTED
    ROUND20 forms (K(a) consecutive-merges-only with the repunit
    digit proof; the census as CORROBORATION). The census numbers
    (if cited at all): 29/80/361/651 surviving records at
    k=5/6/8/9, distinct channels 23/57/218/365, <= 4 real hits per
    drifting channel, the bottom-ladder the only unbounded family
    — corroboration only, NOT load-bearing.
  * OL-2 / S4.3-GENERAL AT FULL ALTITUDE (Lane D rounds 5-8): the
    final ATOM OBLIGATION LAW — any standalone a-block I(u,v)
    (u >= 1, or u = 0 with v < k) demanded in an R/P position sits
    at depth >= (min(v,k-u) - 1 - floor(log_3 M))/2 with
    M <= Q(8#S + O_V(1)) + (v-u+1) + O(log k): Omega(min(v,k-u))
    SURVIVES. The G2 mass form (the slack is the raw digit mass M,
    the transition at n = 7, the witness 7 I(0,1)+8 = I(2,3)); the
    C = 2 closing. CONVENTION (Lane D's final fix, my flag): the
    collected form is Sum_d mu_d 3^d = 0 with mu_d collecting the
    strata coefficients, the target's repunit digits, AND the raw
    ternary digits of the collected low-scale material gamma =
    Sum_p s_p(A_p k + B_p + beta_p) + t (gamma names the value of
    the material inside mu; |gamma| = O(k) enters only the gap
    bound 3^{g-1} <= DM above the EPT boundary log_3(2D|gamma|)).
  * The demand side is COMPLETE: every path by which a separator of
    rev(D(k;3)) reaches its mirror depth is priced — the plant
    window (two-sided match, drift-robust, deficit < 3^u), the mass
    debt ((3^k+1)/2 top-scale, MR/Payment), or the survivor supply
    (Lemma INH). The conditionality ledger: TL/PO, part-B caps,
    Payment, D1-D3/MR/INH, S4.3-general — ALL LANDED.

The pinned schema on B >= 3 now stands at: TL (UNCONDITIONAL,
above); L2.1 slope-pinning + L2.2 per-node supply (Lane D,
PROVED); flankcap (Lane C round 18, PROVED); Payment (Lane B,
proved); SB/DECOMP/FP (proved); D1 F-channel order preservation
(Lane D round 4, PROVED — my fresh evaluator replayed 1594/0). D1''
is machine-supported with an incomplete proof — it must NOT enter
the paper as proved; the proved form is D1''-weak (non-decreasing
labels + slice bound + per-label exhaustion) + the mod-3 T1-trace
(Lane D round 5, proved).

## Required fixes to the paper (from my frontier audit + B5/D4)

1. FAMILY OVERCLAIM (must fix): thm:dichotomy (ii) says "each E
   fails on the super-increasing inputs beyond separator count
   k > f(|E|)". Base 2 IS super-increasing (2^j > 2^j - 1) and is
   the degenerate boundary (dich:prop:boundary: extraction free,
   uniform rev on the B=2 family OPEN — Lane C round 19 landed
   NEGATIVE/architectural: the offset-cost ceiling). Commit the
   family: the strongly super-increasing D(k;3) (any fixed B >= 3
   works), i.e. "fails on the strongly super-increasing inputs".
   Part (ii)'s conclusion is unchanged (D(k;3) is a subfamily of
   {a,b}*).
2. Architecture paragraph (the one before thm:dichotomy): the FP
   construction-side proof is on the base-2 powers — keep (it is
   proved), but ADD the B=2 degeneracy as the recorded reason the
   staging family is base 3 (mount dich:prop:boundary with the
   round-19 update: at B=2 every VALUE is cheap but the ORDER is
   priced, Theta(k) from both free ends).
3. Two-lemma paragraph after the proof — THE B5 CORRECTIONS (all
   verified by me, 54/54 fresh encodings) + THE ROUND-9 TL BLOCK:
   (a) MOUNT LANE B'S ROUND-9 LATEX (ROUND9_REPORT.md section 1,
       thm:TL + its remark) — it carries the two-color profile
       clause, the replication-corrected Delta_V expansion, clause
       (i)'s ambient anchoring, the honest ledger M <= 8*#S +
       O_V(1) with first/last M <= 2, the k-affine + T_V-periodic
       junk, the two-tier directive count, and the mask Boolean
       closure. Constant decision (mine): keep BOTH 8*#S+O_V(1)
       (clause ii, run level) and (d+2)*M (grammar clause) — do
       NOT unify.
   (b) the closure form gains the k-AFFINE JUNK STRATUM: runs are
       Sum q_i 3^{e_i} + (Ak+B) + beta — NOT Q(S) alone; E_last on
       B=3 = (S+k+1)/2 is the witness (the +k/2 is irreducible;
       as stated in round 4 the form was falsified by it);
   (c) RATIONAL SLOPES with V-fixed denominators (tiling
       corrections |R|_a/p; [a/'aaa']X runs (2^j+2)/3, (2^j+4)/3 are
       non-dyadic witnesses) — and the strengthened supply/demand
       mismatch (non-dyadic supply never equals dyadic demand);
   (d) the ledger coefficient is c*#S + O_V(1) with c <= 8, NOT
       #S + O(1) — state the honest constant;
   (e) BA's class list gains the count-times-S product class
       (E_prod's last run 1 + floor(3^k/2)*S is in none of the
       three round-4 classes);
   (f) the site terms are 3^t on D(k;3) (E_leak/E_prod B=3 forms:
       runs (S+3^j, ..., 3^k) and S(3^j-1)/2 + 1).
4. The demand side (Lane D rounds 4-8 + Lane C rounds 20-21):
   restructure to the CHANNEL analysis — D1 (proved), D1''-WEAK +
   the mod-3 T1-trace (proved), the corrected FIRED-SET structure
   (the recursion theorem c_{sigma+1} = R_{sigma+1} - j*[fire];
   SKIP-AFTER-BITE; COR-A/B/B1; class-escape), the T1-trace, the
   engine autopsy as machine-verified facts, the round-4 corollary
   "value > 3^lambda implies T2/T3" WITHDRAWN, the OL-1
   match-exactness core (Lemma M/FIT, K2's real-channel rigidity,
   Lemma S's forcing with the exact deficit BotSum(u-1)), Theorem
   D (D1/D2/D3/MR), Lemma INH, the TRICHOTOMY (a) per-pattern
   scale slots (flankcap), (b) deep tuned intermediates (OL-2 =
   the atom obligation law at full altitude), (c) mergey pollution
   (Payment), and the DISJUNCTIVE counting corollary (both branches
   linear). NO REMAINING CONDITIONALITY: all dependencies landed
   and are cited above.
5. thm:dichotomy (ii) status: FLIP TO PROVED. "Conditional on the
   two structure lemmas" becomes the proved statement with the
   effective bound k > c_E #S(E) + c'_E (linear in pass count);
   keep the tightness remark (engine Theta(k) depth, Theta(k^2)
   size). The honest residue to note (one sentence each, in the
   record not the theorem): the (beta)-level plant interface is
   carried by the supply branch (stated, Lane C round 21 section 5);
   S4.3-general's swept domains are EPT-licensed to the general
   anchored families; the -O(log k) additive slack and the tight
   constant C in [2,4] are open refinement questions, not gaps.
6. Update the integration-hook comment (line ~2626) — the round is
   no longer in flight.
7. Update the frontier lead-in (line ~2421) and the closing sentence
   of the toll paragraph (line ~2611) to the final statuses.
8. SS19 of research/scratch/rev/REPORT.md: the endgame record
   (rounds 15C-21, the B=2 boundary, the ledger theorem and its two
   corrections, the demand-side channels, Theorem D, the atom
   obligation law, the residue map).
9. Full compile: 0 errors, 0 undefined refs, 0 overfull — report
   page count. No new unverified claims: every integrated statement
   must trace to my OVERVIEW entries.

## Standing content (from the verified record)

- thm:dichotomy (i) stays (fixed separator structures computable,
  laundering k <= 5); (iii) stays (unbounded alphabets).
- The FP proof sketch's machine backdrop line stays as-is (all
  stats verified).
- Lane C's round-19 side quest (B=2 uniform rev): NEGATIVE
  (architectural), verified: the OFFSET-COST CEILING. The boundary
  proposition (dich:prop:boundary) UPDATED: at B=2 every VALUE on
  the telescoping family is cheap (paddings, products, one-pass
  scalings by 2^k, the plant-depth ladder in two passes) but the
  ORDER is priced: reversal needs the middle offsets, Theta(k)
  from both free ends; five tools machine-verified (STEPDOWN,
  JUMPDOWN, SCALER, DIVIDER, LADDER); both +/-S padding directions
  round-trip (pad-then-strip dead); the rotation route dies at
  k=3. Uniform rev on the B=2 family REMAINS OPEN but
  census-backed obstructed; the dichotomy's linear f(|E|)
  survives the boundary intact.

## Rules (user directives, hard)

- Every claim traces to my OVERVIEW entries or the artifacts listed
  there; nothing unverified enters the paper; full-compile check;
  report back to me for verification.
