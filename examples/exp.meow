true() {"T"}

false() {"F"}

zero() {"0"}

succ(x) {cat "S" x}

pred(x) {
    let "S0" = "0" x
}

dr(x,y,z) {
    let y = x let x = y z
}

encode(s) {
    cat cat "_^"
        let "$" = "\$"
        let "^" = "\^"
        let "\" = "\\"
        s
    "_$"
}
